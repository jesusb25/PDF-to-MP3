{
  "name": "PDF-to-MP3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix web && npm run build --prefix web",
    "start": "node server/server.js",
    "dev": "concurrently -n server,web -c blue,green \"nodemon server/server.js\" \"npm run dev --prefix web\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.22.2",
    "express-fileupload": "^1.4.0",
    "express-rate-limit": "^7.5.1",
    "google-tts-api": "^2.0.2",
    "pdfjs-dist": "^4.10.38"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "dotenv": "^16.3.1",
    "nodemon": "^3.0.1"
  },
  "overrides": {
    "google-tts-api": {
      "axios": "^1.13.5"
    }
  }
}
